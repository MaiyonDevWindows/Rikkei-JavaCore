# Xoá các file trong .gitignore trên remote repository.
git ls-files -i -c --exclude-from=.gitignore | %{git rm --cached $_}
git add .
git commit -m "Remove files from .gitignore"
git push origin main